<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <style>
        /* Import Google font - Poppins */
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400;500;600;700&display=swap");
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }
        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            background: rgb(130, 106, 251);
            background-image: url('https://cdn.glitch.global/f38667d5-b94e-4d97-81d2-ff097b3b2247/2pro.jpg?v=1696502145749');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        .b::before{
            content: "";
            position: absolute;
            top:0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
        }      
        .container {
            position: relative;
            max-width: 700px;
            width: 100%;
            background: rgba(0, 0, 0, 0.8);
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            color: #fff;
        }
        .container header {
            font-size: 1.5rem;
            color: #fff;
            font-weight: 500;
            text-align: center;
        }
        .container .form {
            margin-top: 30px;
        }
        .form .input-box {
            width: 100%;
            margin-top: 20px;
        }
        .input-box label {
            color: #fff;
        }
        .form :where(.input-box input, .select-box) {
            position: relative;
            height: 50px;
            width: 100%;
            outline: none;
            font-size: 1rem;
            color: #fff;
            margin-top: 8px;
            border: 1px solid #ddd;
            border-radius: 6px;
            padding: 0 15px;
            background: rgba(0, 0, 0, 0.2);
        }
        .input-box input:focus {
            box-shadow: 0 1px 0 rgba(0, 0, 0, 0.1);
        }
        .form .column {
            display: flex;
            column-gap: 15px;
        }
        .form .gender-box {
            margin-top: 20px;
        }
        .gender-box h3 {
            color: #fff;
            font-size: 1rem;
            font-weight: 400;
            margin-bottom: 8px;
        }
        .form :where(.gender-option, .gender) {
            display: flex;
            align-items: center;
            column-gap: 50px;
            flex-wrap: wrap;
        }
        .form .gender {
            column-gap: 5px;
        }
        .gender input {
            accent-color: rgb(130, 106, 251);
        }
        .form :where(.gender input, .gender label) {
            cursor: pointer;
        }
        .gender label {
            color: #fff;
        }
        .address :where(input, .select-box) {
            margin-top: 15px;
        }
        /* Custom styling for select elements */
        .select-box select {
            height: 50px;
            width: 100%;
            outline: none;
            border: none;
            color: #fff;
            font-size: 1rem;
            background: rgba(0, 0, 0, 0.2);
        }
        .form select:focus {
            background: rgba(0, 0, 0, 0.6); /* Transparent black background when focused */
        }
        .form select option {
            background: rgba(0, 0, 0, 0.6); /* Transparent black background for options */
            color: #fff;
        }
        /* Customize the calendar icon */
        .form input[type="date"]::-webkit-calendar-picker-indicator {
            background: rgba(0, 0, 0, 0.6); /* Transparent black background for the calendar icon */
        }
        .form button {
            height: 55px;
            width: 100%;
            color: #fff;
            font-size: 1rem;
            font-weight: 400;
            margin-top: 30px;
            border: none;
            cursor: pointer;
            transition: all 0.2s ease;
            background: rgb(255, 87, 51);
        }
        .form button:hover {
            background: rgb(255, 87, 51);
        }
        @media screen and (max-width: 500px) {
            .form .column {
                flex-wrap: wrap;
            }
            .form :where(.gender-option, .gender) {
                row-gap: 15px;
            }
        }
        .logo {
            position: absolute;
            top: 5px;
            left: 10px;
            width: 160px;
            height: auto;
        }
    </style>
</head>
<body>
    <section class="container">
        <header>Sign-In Form</header>
        <form id="signin-form" action="/signinsubmit" method="GET" class="form">
            <div class="input-box">
                <label>Email Address</label>
                <input type="text" name="email" placeholder="Enter email address" required />
            </div>
            <div class="input-box">
                <label>Password</label>
                <input type="password" name="password" placeholder="Enter your password" required />
            </div>
            <button type="submit">Sign In</button>
        </form>
    </section>
    <div class="logo">
        <img src="https://cdn.glitch.global/f38667d5-b94e-4d97-81d2-ff097b3b2247/erebfyvy-removebg-preview.png?v=1696487966062" alt="Your Logo" class="logo">
    </div>
</body>
</html>
